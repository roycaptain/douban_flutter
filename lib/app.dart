import 'package:flutter/material.dart';
import 'package:douban_app/movie_page.dart';

class App extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        // TODO: implement build
        return MaterialApp(
            home: MovieScreen(),
        );
    }
}